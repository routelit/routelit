/// <reference path="../types.d.ts" />
// Make TypeScript recognize Jest-DOM matchers
import '@testing-library/jest-dom';

// This file is used by Jest to setup the testing environment
// It's referenced in the jest.config.js file under setupFilesAfterEnv

// This will be run before each test
beforeEach(() => {
  // You can add global setup code here if needed
});
